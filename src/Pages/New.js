import NewTransaction from "../Components/NewTransaction";

const New = () => {
    return (
        <div>
            <h1>Make a new transaction</h1>
            <NewTransaction />
        </div>
    );
};

export default New;